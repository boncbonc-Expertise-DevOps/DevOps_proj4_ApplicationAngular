apiVersion: v1
kind: Service
metadata:
  name: {{ include "olympic-tracker-chart.fullname" . }}
  labels:
    {{- include "olympic-tracker-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "olympic-tracker-chart.selectorLabels" . | nindent 4 }}
